{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Server",
      "program": "${workspaceFolder}/src/server.ts",
      "preLaunchTask": "build",
      "outFiles": [
        "${workspaceFolder}/dist/server.js"
      ]
    }
  ]
}